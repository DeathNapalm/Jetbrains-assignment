# Performance Reporting

Parse the latest Gatling simulation logs into CSV and SVG report artifacts:

```bash
python3 scripts/performance_reporting/parse_results.py
```

Outputs are written to `perf_test_scripts/results/parsed/`.