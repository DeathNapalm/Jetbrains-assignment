# YouTrack Statistics

Report project, user, and issue counts for a running YouTrack instance:

```bash
scripts/youtrack_statistics/yt_stats.sh
```

Set `YOUTRACK_TOKEN` directly or create `scripts/test_data_generation/.env`.